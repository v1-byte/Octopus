(()=>{const $=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));const key='octopus_symbol_meta',load=()=>JSON.parse(localStorage.getItem(key)||'{}'),save=x=>localStorage.setItem(key,JSON.stringify(x));let meta=load();function symbols(){return[...document.querySelectorAll('#symbolGrid input[data-key="label"]')].map(x=>({id:x.dataset.symbol,label:x.value||x.dataset.symbol}))}function renderSelect(){const e=$('#visualSymbolSelect');if(!e)return;const ss=symbols();e.innerHTML=ss.length?ss.map(x=>`<option value="${esc(x.id)}">${esc(x.label)}</option>`).join(''):'<option value="">Belum ada symbol</option>';loadEditor()}function current(){const id=$('#visualSymbolSelect').value;meta[id]??={w:64,h:64,x:0,y:0,rotation:0,scale:1,opacity:1,layer:0,animation:'IDLE',sound:'',locked:false};return meta[id]}function loadEditor(){const m=current();if(!m)return;for(const [id,v] of Object.entries({visualW:m.w,visualH:m.h,visualX:m.x,visualY:m.y,visualRotation:m.rotation,visualScale:m.scale,visualOpacity:m.opacity,visualLayer:m.layer,visualAnimation:m.animation,visualSound:m.sound}))if($('#'+id))$('#'+id).value=v;$('#visualLock').checked=!!m.locked}function saveEditor(){const id=$('#visualSymbolSelect').value;if(!id)return;const m=current();Object.assign(m,{w:Number($('#visualW').value)||64,h:Number($('#visualH').value)||64,x:Number($('#visualX').value)||0,y:Number($('#visualY').value)||0,rotation:Number($('#visualRotation').value)||0,scale:Number($('#visualScale').value)||1,opacity:Number($('#visualOpacity').value)||1,layer:Number($('#visualLayer').value)||0,animation:$('#visualAnimation').value,sound:$('#visualSound').value,locked:$('#visualLock').checked});save(meta);toast('Konfigurasi visual symbol tersimpan')}function toast(m){const e=$('#toast');e.textContent=m;e.classList.add('show');setTimeout(()=>e.classList.remove('show'),2200)}function bind(){$('#visualSymbolSelect').onchange=loadEditor;$('#saveVisualBtn').onclick=saveEditor;$('#duplicateVisualBtn').onclick=()=>{const id=$('#visualSymbolSelect').value;if(!id)return;const copy={...current(),locked:false};meta[id+'_copy']=copy;save(meta);toast('Preset visual diduplikasi')};const grid=$('#symbolGrid');new MutationObserver(renderSelect).observe(grid,{childList:true,subtree:true});renderSelect()}bind();window.OctopusSymbolEngine={metadata:()=>meta,refresh:renderSelect}})();