(()=>{let adapter=null;const finite=x=>Number.isFinite(Number(x));function validate(profile){if(!profile||profile.mode!=='live-test')throw new Error('Profil harus memakai mode live-test.');if(!Array.isArray(profile.symbols)||!profile.symbols.length)throw new Error('Profil belum memiliki symbol.');if(!finite(profile.seed)||!Number.isInteger(Number(profile.seed)))throw new Error('Seed harus berupa integer.');for(const symbol of profile.symbols){if(!symbol.name||!Array.isArray(symbol.weights)||symbol.weights.length!==5)throw new Error('Setiap symbol wajib memiliki 5 bobot custom.');if(symbol.weights.some(weight=>!finite(weight)||Number(weight)<0))throw new Error('Bobot symbol harus angka nol atau lebih.')}return true}function register(next){if(!next||typeof next.applyProfile!=='function'||typeof next.revokeProfile!=='function')throw new Error('Adapter wajib memiliki applyProfile() dan revokeProfile().');adapter={name:String(next.name||'Custom Live Test Adapter'),applyProfile:next.applyProfile,revokeProfile:next.revokeProfile};return status()}async function applyProfile(profile){validate(profile);if(!adapter)throw new Error('Custom live test adapter belum terdaftar.');return await adapter.applyProfile(JSON.parse(JSON.stringify(profile)))}async function revokeProfile(){if(!adapter)throw new Error('Custom live test adapter belum terdaftar.');return await adapter.revokeProfile()}function status(){return{registered:Boolean(adapter),name:adapter?.name||null,mode:'authorized-live-test',requiresServerAdapter:true}}window.OctopusLiveTestSDK={register,applyProfile,revokeProfile,status,validate}})();