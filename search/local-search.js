(()=>{let docs=[];const norm=s=>String(s||'').toLowerCase().normalize('NFKD').replace(/[\u0300-\u036f]/g,'');const load=async()=>{if(docs.length)return;try{docs=(await fetch('search/local-index.json').then(r=>r.json())).documents||[]}catch(e){docs=[]}};window.OctopusLocalSearch={search:async(q,type='web')=>{await load();const terms=norm(q).split(/\s+/).filter(Boolean);return docs.map(d=>{const text=norm([d.title,d.description,d.content].join(' '));const score=terms.reduce((n,t)=>n+(text.includes(t)?(norm(d.title).includes(t)?4:1):0),0);return{...d,score,displayUrl:d.url}}).filter(d=>d.score>0).sort((a,b)=>b.score-a.score).slice(0,10)}}})();